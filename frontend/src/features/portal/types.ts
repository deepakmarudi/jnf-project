export type PortalHighlight = {
  title: string;
  description: string;
};

export type PortalProcessStep = {
  step: string;
  title: string;
  description: string;
};

export type PortalLinkItem = {
  title: string;
  href: string;
  external: boolean;
};

export type PortalContactDetails = {
  title: string;
  description: string;
  organization: string;
  website: string;
};
