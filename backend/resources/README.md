# Resources

This backend is API-first, so `resources/` is currently reserved for:

- mail views
- exported templates
- server-rendered fallback views if needed later

Do not place business logic in this folder.
